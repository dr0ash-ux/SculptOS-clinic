import { FormEvent, useEffect, useMemo, useState } from 'react'
import { AlertTriangle, ArrowDownToLine, History, Package, Pencil, Plus, Search, Truck, X } from 'lucide-react'
import { supabase } from './lib/supabase'

type Workspace = { organizationId: string; clinicId: string; clinicName: string; role: string }
type Item = { id:string; clinic_id:string; name:string; category:string; sku:string|null; unit:string; reorder_threshold:number; current_stock:number; active:boolean; default_unit_cost:number; supplier:string|null; batch_number:string|null; expiry_date:string|null; storage_notes:string|null; description:string|null }
type Movement = { id:string; movement_type:string; total_value:number; movement_date:string }
type Form = { name:string; category:string; sku:string; unit:string; reorder:string; cost:string; supplier:string; batch:string; expiry:string; storage:string; description:string; active:boolean }
const categories=['Consumables','Medicines','Disposables','Instruments','Other']
const blank=():Form=>({name:'',category:'Consumables',sku:'',unit:'piece',reorder:'0',cost:'0',supplier:'',batch:'',expiry:'',storage:'',description:'',active:true})
const fromItem=(item:Item):Form=>({name:item.name,category:item.category,sku:item.sku||'',unit:item.unit,reorder:String(item.reorder_threshold),cost:String(item.default_unit_cost),supplier:item.supplier||'',batch:item.batch_number||'',expiry:item.expiry_date||'',storage:item.storage_notes||'',description:item.description||'',active:item.active})
const money=(n:number)=>`₹${Number(n||0).toLocaleString('en-IN',{maximumFractionDigits:2})}`
const startMonth=()=>{const d=new Date();return new Date(d.getFullYear(),d.getMonth(),1).toISOString().slice(0,10)}

export function InventoryPage({workspace,onNotice}:{workspace:Workspace;onNotice:(message:string)=>void}){
 const [items,setItems]=useState<Item[]>([]),[moves,setMoves]=useState<Movement[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState(''),[query,setQuery]=useState(''),[category,setCategory]=useState('all'),[stock,setStock]=useState('all')
 const [form,setForm]=useState<Form>(blank),[editing,setEditing]=useState<Item|null>(null),[itemOpen,setItemOpen]=useState(false),[picker,setPicker]=useState<'usage'|'receipt'|null>(null),[selected,setSelected]=useState<Item|null>(null)
 const canManage=['admin','manager','receptionist'].includes(workspace.role)
 const load=async()=>{setLoading(true);setError('');const [a,b]=await Promise.all([supabase.from('inventory_items').select('id,clinic_id,name,category,sku,unit,reorder_threshold,current_stock,active,default_unit_cost,supplier,batch_number,expiry_date,storage_notes,description').eq('clinic_id',workspace.clinicId).order('name'),supabase.from('inventory_stock_movements').select('id,movement_type,total_value,movement_date').gte('movement_date',startMonth())]);const message=a.error?.message||b.error?.message;if(message){setError(message);onNotice(message)}setItems((a.data||[])as Item[]);setMoves((b.data||[])as Movement[]);setLoading(false)}
 useEffect(()=>{void load()},[workspace.clinicId])
 const shown=useMemo(()=>items.filter(i=>{const low=i.current_stock<=i.reorder_threshold;return `${i.name} ${i.category} ${i.sku||''} ${i.supplier||''}`.toLowerCase().includes(query.toLowerCase())&&(category==='all'||i.category===category)&&(stock==='all'||stock==='low'?low:!low)}),[items,query,category,stock])
 const active=items.filter(i=>i.active),low=active.filter(i=>i.current_stock<=i.reorder_threshold).length,value=active.reduce((n,i)=>n+i.current_stock*i.default_unit_cost,0),usage=moves.filter(i=>i.movement_type==='usage').reduce((n,i)=>n+Math.abs(Number(i.total_value||0)),0)
 const openAdd=()=>{setEditing(null);setForm(blank());setItemOpen(true)}
 const edit=(item:Item)=>{setEditing(item);setForm(fromItem(item));setItemOpen(true)}
 const saveItem=async(e:FormEvent)=>{e.preventDefault();if(!form.name.trim())return;const user=(await supabase.auth.getUser()).data.user;const data={name:form.name.trim(),category:form.category,sku:form.sku.trim()||null,unit:form.unit||'piece',reorder_threshold:Number(form.reorder||0),default_unit_cost:Number(form.cost||0),supplier:form.supplier.trim()||null,batch_number:form.batch.trim()||null,expiry_date:form.expiry||null,storage_notes:form.storage.trim()||null,description:form.description.trim()||null,active:form.active,updated_by:user?.id};const result=editing?await supabase.from('inventory_items').update(data).eq('id',editing.id).eq('clinic_id',workspace.clinicId):await supabase.from('inventory_items').insert({...data,clinic_id:workspace.clinicId,created_by:user?.id});if(result.error){onNotice(result.error.message);return}setItemOpen(false);onNotice(editing?'Inventory item updated.':'Inventory item created. Use Receive stock to add quantity.');await load()}
 const record=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();if(!selected||!picker)return;const f=new FormData(e.currentTarget),q=Number(f.get('quantity')||0),date=String(f.get('date')||new Date().toISOString().slice(0,10)),reason=String(f.get('reason')||'').trim()||null;if(q<=0)return;const user=(await supabase.auth.getUser()).data.user;if(picker==='receipt'){const cost=Number(f.get('cost')||selected.default_unit_cost||0),supplier=String(f.get('supplier')||selected.supplier||'').trim()||null;const purchase=await supabase.from('inventory_purchases').insert({clinic_id:workspace.clinicId,supplier,purchased_on:date,total_amount:q*cost,created_by:user?.id}).select('id').single();if(purchase.error||!purchase.data){onNotice(purchase.error?.message||'Could not record purchase.');return}const result=await supabase.from('inventory_stock_movements').insert({item_id:selected.id,inventory_purchase_id:purchase.data.id,movement_type:'purchase',quantity:q,unit_cost_snapshot:cost,movement_date:date,reason:reason||'Stock received',created_by:user?.id});if(result.error){onNotice(result.error.message);return}onNotice('Stock received. Procurement expense was added to Finance.')}else{const result=await supabase.from('inventory_stock_movements').insert({item_id:selected.id,movement_type:String(f.get('type')||'usage'),quantity:q,unit_cost_snapshot:selected.default_unit_cost,movement_date:date,reason,created_by:user?.id});if(result.error){onNotice(result.error.message);return}onNotice('Stock movement recorded.')}setSelected(null);setPicker(null);await load()}
 const select=(item:Item)=>setSelected(item)
 return <section className="inventory-page"><div className="hero-row"><div><span className="eyebrow">ADMIN · INVENTORY</span><h1>Inventory</h1><p className="muted">Track clinical materials, medicines and consumables from stock receipt through patient and procedure-level usage.</p></div><div className="inventory-actions"><button className="ghost" onClick={()=>void load()}><History size={16}/>Refresh</button>{canManage&&<><button className="ghost" disabled={!active.length} onClick={()=>setPicker('usage')}><ArrowDownToLine size={16}/>Record usage</button><button className="ghost" disabled={!active.length} onClick={()=>setPicker('receipt')}><Truck size={16}/>Receive stock</button><button className="primary" onClick={openAdd}><Plus size={16}/>Add item</button></>}</div></div><div className="metric-grid"><Metric label="Active items" value={String(active.length)} hint={low?`${low} need reorder`:'All items above threshold'} icon={<Package size={17}/>}/><Metric label="Low-stock items" value={String(low)} hint="At or below reorder level" icon={<AlertTriangle size={17}/>}/><Metric label="Stock value" value={money(value)} hint="At saved unit costs" icon={<Package size={17}/>}/><Metric label="Usage this month" value={money(usage)} hint="Recorded clinical usage" icon={<ArrowDownToLine size={17}/>}/></div><div className="panel inventory-panel"><div className="panel-head inventory-toolbar"><div><h3>Stock catalogue</h3><span>{shown.length} items in this clinic</span></div><div className="inventory-filters"><label className="inventory-search"><Search size={15}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search items, SKU or supplier"/></label><select value={category} onChange={e=>setCategory(e.target.value)}><option value="all">All categories</option>{categories.map(x=><option key={x}>{x}</option>)}</select><select value={stock} onChange={e=>setStock(e.target.value)}><option value="all">All stock status</option><option value="low">Low stock</option><option value="healthy">Healthy stock</option></select></div></div>{loading&&<div className="empty-state"><p>Loading inventory…</p></div>}{!loading&&error&&<div className="empty-state"><AlertTriangle size={20}/><p>{error}</p><button className="ghost small" onClick={()=>void load()}>Try again</button></div>}{!loading&&!error&&!shown.length&&<div className="empty-state"><Package size={22}/><p>No inventory items match these filters.</p></div>}{!loading&&!error&&!!shown.length&&<div className="inventory-table-wrap"><div className="inventory-native-table"><div className="inventory-native-row inventory-native-head"><span>Item</span><span>Category</span><span>Current stock</span><span>Reorder level</span><span>Batch / expiry</span><span>Supplier</span><span/></div>{shown.map(i=>{const isLow=i.active&&i.current_stock<=i.reorder_threshold;return <div className={i.active?'inventory-native-row':'inventory-native-row inactive'} key={i.id}><span><b>{i.name}</b><small>{i.sku||'No SKU'} · {i.unit}{!i.active?' · Inactive':''}</small></span><span><em className="inventory-badge">{i.category}</em></span><span><b>{i.current_stock.toLocaleString('en-IN')}</b> <small>{i.unit}</small>{isLow&&<em className="inventory-low">LOW STOCK</em>}</span><span>{i.reorder_threshold} {i.unit}</span><span>{i.batch_number||'—'}{i.expiry_date&&<small>Exp {new Date(i.expiry_date).toLocaleDateString('en-IN')}</small>}</span><span>{i.supplier||'—'}</span><span className="inventory-row-actions">{canManage&&<><button className="ghost small" onClick={()=>{setPicker('usage');select(i)}}>Use</button><button className="ghost small" onClick={()=>edit(i)} aria-label={`Edit ${i.name}`}><Pencil size={14}/></button></>}</span></div>})}</div></div>}</div>{itemOpen&&<div className="inventory-modal-backdrop" onMouseDown={e=>e.target===e.currentTarget&&setItemOpen(false)}><form className="panel inventory-modal" onSubmit={saveItem}><div className="panel-head"><div><h3>{editing?'Edit inventory item':'Add inventory item'}</h3><span>Stock quantities are changed through receipts and movements.</span></div><button type="button" className="icon-btn" onClick={()=>setItemOpen(false)}><X size={17}/></button></div><Fields form={form} setForm={setForm}/><div className="treatment-switches"><label><input type="checkbox" checked={form.active} onChange={e=>setForm({...form,active:e.target.checked})}/>Active and available for stock activity</label></div><div className="settings-actions"><button type="button" className="ghost" onClick={()=>setItemOpen(false)}>Cancel</button><button className="primary">{editing?'Save changes':'Create item'}</button></div></form></div>}{picker&&!selected&&<Picker mode={picker} items={active} onClose={()=>setPicker(null)} onPick={select}/>} {picker&&selected&&<Movement item={selected} mode={picker} onClose={()=>{setSelected(null);setPicker(null)}} onSubmit={record}/>}</section>
}
function Fields({form,setForm}:{form:Form;setForm:(x:Form)=>void}){return <div className="form-grid two"><label>Item name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Category<select value={form.category} onChange={e=>setForm({...form,category:e.target.value})}>{categories.map(x=><option key={x}>{x}</option>)}</select></label><label>SKU / internal code<input value={form.sku} onChange={e=>setForm({...form,sku:e.target.value})}/></label><label>Unit<input value={form.unit} onChange={e=>setForm({...form,unit:e.target.value})}/></label><label>Reorder level<input type="number" min="0" step="0.001" value={form.reorder} onChange={e=>setForm({...form,reorder:e.target.value})}/></label><label>Default unit cost<input type="number" min="0" step="0.01" value={form.cost} onChange={e=>setForm({...form,cost:e.target.value})}/></label><label>Supplier<input value={form.supplier} onChange={e=>setForm({...form,supplier:e.target.value})}/></label><label>Batch number<input value={form.batch} onChange={e=>setForm({...form,batch:e.target.value})}/></label><label>Expiry date<input type="date" value={form.expiry} onChange={e=>setForm({...form,expiry:e.target.value})}/></label><label className="inventory-full-field">Storage notes<textarea value={form.storage} onChange={e=>setForm({...form,storage:e.target.value})}/></label><label className="inventory-full-field">Description<textarea value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></label></div>}
function Picker({mode,items,onClose,onPick}:{mode:'usage'|'receipt';items:Item[];onClose:()=>void;onPick:(i:Item)=>void}){return <div className="inventory-modal-backdrop" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><div className="panel inventory-modal compact-modal"><div className="panel-head"><div><h3>{mode==='receipt'?'Receive new stock':'Record stock usage'}</h3><span>Select an item.</span></div><button className="icon-btn" onClick={onClose}><X size={17}/></button></div><div className="inventory-picker-list">{items.map(i=><button className="inventory-picker-row" type="button" key={i.id} onClick={()=>onPick(i)}><span><b>{i.name}</b><small>{i.current_stock} {i.unit} available · {i.category}</small></span><ArrowDownToLine size={16}/></button>)}</div></div></div>}
function Movement({item,mode,onClose,onSubmit}:{item:Item;mode:'usage'|'receipt';onClose:()=>void;onSubmit:(e:FormEvent<HTMLFormElement>)=>void}){const today=new Date().toISOString().slice(0,10);return <div className="inventory-modal-backdrop" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><form className="panel inventory-modal compact-modal" onSubmit={onSubmit}><div className="panel-head"><div><h3>{mode==='receipt'?'Receive stock':'Record stock usage'}</h3><span>{item.name} · {item.current_stock} {item.unit} available</span></div><button className="icon-btn" type="button" onClick={onClose}><X size={17}/></button></div><div className="form-grid two">{mode==='usage'&&<label>Movement<select name="type" defaultValue="usage"><option value="usage">Clinical usage</option><option value="wastage">Wastage</option><option value="return">Return / removal</option><option value="adjustment">Stock count adjustment</option></select></label>}<label>{mode==='receipt'?'Quantity received':'Quantity'}<input name="quantity" required type="number" min="0.001" step="0.001"/></label><label>Date<input name="date" type="date" defaultValue={today}/></label>{mode==='receipt'&&<><label>Unit cost<input name="cost" required type="number" min="0" step="0.01" defaultValue={item.default_unit_cost}/></label><label>Supplier<input name="supplier" defaultValue={item.supplier||''}/></label></>}<label className="inventory-full-field">Reason / note<input name="reason" placeholder={mode==='receipt'?'Purchase order or invoice reference':'e.g. RCT 16, expiry or count correction'}/></label></div><div className="settings-actions"><button className="ghost" type="button" onClick={onClose}>Cancel</button><button className="primary">{mode==='receipt'?<><Truck size={15}/>Receive stock</>:<><ArrowDownToLine size={15}/>Record movement</>}</button></div></form></div>}
function Metric({label,value,hint,icon}:{label:string;value:string;hint:string;icon:React.ReactNode}){return <div className="metric"><div className="metric-icon">{icon}</div><span>{label}</span><b>{value}</b><small className="metric-hint">{hint}</small></div>}
